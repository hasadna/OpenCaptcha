language:
  python

python:
  - 3.7
  - 3.8

#matrix:
#  include:
#    - env: TOXENV=lint

env:
  global:
    - TOXENV="py${PYTHON_VERSION//./}"

install:
  - pip install --upgrade -e .[develop]
  - pip install coveralls

script:
  - tox

after_success:
  - coveralls
